NestLedger frontend is served by Flask from backend/app.py.
Open http://127.0.0.1:5000 after starting the backend.

The frontend is vanilla HTML/CSS/JavaScript and uses the /api endpoints.
Razorpay Checkout is loaded from the official Razorpay checkout script.
